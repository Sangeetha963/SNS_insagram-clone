import { ClientLocalAuthGuard } from './client-local-auth.guard'

describe('ClientLocalAuthGuard', () => {
    it('should be defined', () => {
        expect(new ClientLocalAuthGuard()).toBeDefined()
    })
})
